/* Minimal SockJS shim for local demo. In real app include official SockJS client. */
function SockJS(url) {
    this.url = url;
    this.onopen = null;
    this._listeners = {};
    setTimeout(() => { if (this.onopen) this.onopen(); }, 10);
    // deliver messages via global demo helper if present
}
SockJS.prototype.send = function(){};
